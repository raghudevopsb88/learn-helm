apiVersion: v1
kind: ConfigMap
metadata:
  name: helm-02
data:
  toppings: |-
    {{- range .Values.pizzaToppings }}
    - {{ . | title | quote }}
    - {{ $.Values.my_number }}
    {{- end }}

# .Values inside the loop, Need to change that to $.Values
